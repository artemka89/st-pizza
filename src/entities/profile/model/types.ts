export interface ProfileDto {
  $id: string;
  phone: string;
  imageId: string;
  city: string;
  street: string;
}

export interface Profile {
  id: string;
  phone: string;
  imageId: string;
  city: string;
  street: string;
}
